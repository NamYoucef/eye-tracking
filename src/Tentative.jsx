import React from 'react';
import "./index.css";
import { Badge, Button } from 'antd';
import { CloseCircleOutlined } from '@ant-design/icons';

const Tentative = ({ nbError }) => {

    return (
        <div>
            <div className='timer'>
            </div>
        </div>
    );
};

export default Tentative;
